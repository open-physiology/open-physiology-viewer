import {
	chai,
	sinon,
	describe,
	it,
	beforeEach,
	afterEach,
	expect,
} from './test.helper';

/** @test */
describe("the open-physiology-viewer", () => {
	
	beforeEach(() => {
		// setup
	});
	
	it("can test things", () => {
		expect(true).to.be.truthy;
	});

	afterEach(() => {
		// teardown
	});
	
});
