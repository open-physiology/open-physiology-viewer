import {
	chai,
	sinon,
	describe,
	it,
	beforeEach,
	afterEach,
	expect,
} from './test.helper';

/** @test */
describe("the open-physiology-viewer", () => {
	
	beforeEach(() => {
		// setup
	});
	
	it("can test things", () => {
		//expect(false).to.be.truthy;
	});

	afterEach(() => {
		// teardown
	});
	
});
